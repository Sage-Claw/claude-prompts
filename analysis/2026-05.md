# Monthly Prompt Evolution Analysis — April 2026

## Executive Summary

April 2026 was dominated by a Claude Code extraction regression, not normal prompt iteration. We still observed Claude Code binary movement (v2.1.87 → v2.1.121), but the captured prompt body collapsed to an empty-hash stub, which blocks deep semantic diffing for most of the month. Other tracked tools (Cowork, Cline, OpenAI Codex, Aider) showed no April snapshot activity in this repo.

---

## Claude Code Deep Dive

### Versions Released (what we can verify)
- **Observed binary progression:** `v2.1.87` (Mar 30 snapshot) → `v2.1.119` → `v2.1.121` (Apr 28 snapshot)
- **April Claude Code commits in this repo:** 4
- **Distinct April prompt snapshots with files:** 2 (`2.1.119`, `2.1.121`)
- **Caveat:** Because extraction is regressed, repo-level snapshot count understates actual upstream app releases.

### New capabilities/instructions added this month
- **No reliably extractable new instruction text** in April snapshots.
- Latest captured file is a metadata-only shell with:
  - `prompt-hash: e3b0c44298fc1c14`
  - no substantive system prompt body

### Removed or weakened instructions
Compared to the ~30-day-ago snapshot (`v2.1.87`, extracted 2026-03-30), the current file (`v2.1.121`, extracted 2026-04-28) effectively removes visible instruction content from the repository artifact:
- Identity/runtime blocks no longer present in captured body
- System/Coding/Executing-with-care sections absent in captured body
- What remains is only frontmatter + extraction headers

This appears to be an **extraction failure**, not a confirmed intentional Anthropic prompt simplification.

### Philosophy shifts (inference)
- **Observable product direction is ambiguous in April** because the source artifact is mostly blank.
- The **last trustworthy philosophy signal** is still late March (`v2.1.86`): more structured tool use, session isolation via scratchpad, and concise operator-facing behavior.

### Interesting patterns
1. **Version velocity continued despite visibility loss**: app version advanced materially while prompt observability degraded.
2. **`build-time` degraded to `unknown`** in latest snapshots, matching the broader extraction break.
3. **Operational risk**: monthly analysis quality now depends on fixing extractor resilience more than on analyst effort.

---

## Other Tools

### Claude Cowork
- **April repo snapshots/commits:** 0
- **Major philosophy changes observed:** none (no new captured artifacts)

### Cline
- **April repo snapshots/commits:** 0
- **Major philosophy changes observed:** none in this month’s captured data

### OpenAI Codex
- **April repo snapshots/commits:** 0
- **Major philosophy changes observed:** none in this month’s captured data

### Aider
- **April repo snapshots/commits:** 0
- **Major philosophy changes observed:** none in this month’s captured data

---

## Trends

1. **Observability became the bottleneck.** In April, tooling reliability (extraction integrity) mattered more than prompt content changes.
2. **Claude Code remained the only active track in this repo**, but with degraded fidelity.
3. **Cross-tool comparison confidence dropped** because non-Claude tracks had no fresh snapshots.

---

## Notable Quotes

From `claude-code/system-prompt.md` (latest):
- `prompt-hash: e3b0c44298fc1c14`
- `> Extracted from binary \`2.1.121\` (build: \`None\`)`

From `claude-code/system-prompt.md` (~30 days ago, `v2.1.87`):
- `IMPORTANT: Always use this scratchpad directory for temporary files instead of /tmp or other system temp directories:`
- `If an approach fails, diagnose why before switching tactics... Don't retry the identical action blindly, but don't abandon a viable approach after a single failure either.`

From `claude-code/CHANGELOG.md` (last high-confidence semantic entry, Mar 27):
- `Added ! <command> shorthand tip`
- `Added explicit tool-preference rules: use Read instead of cat/head/tail/sed ...`
